<form method="post" action="{{ path('app_nationalities_delete', {'id': nationality.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nationality.id) }}">
    <button class="btn">Delete</button>
</form>
