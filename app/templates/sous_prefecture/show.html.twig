<!DOCTYPE html>

<title>SousPrefecture</title>

{% block body %}
    <h1>SousPrefecture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_prefecture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sous_prefecture.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_prefecture_index') }}">back to list</a>

    <a href="{{ path('app_sous_prefecture_edit', {'id': sous_prefecture.id}) }}">edit</a>

    {{ include('sous_prefecture/_delete_form.html.twig') }}
{% endblock %}
