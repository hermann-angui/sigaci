<form method="post" action="{{ path('app_sous_prefecture_delete', {'id': sous_prefecture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sous_prefecture.id) }}">
    <button class="btn">Delete</button>
</form>
