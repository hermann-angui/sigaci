<!DOCTYPE html>

<title>CorpsMetiers</title>

{% block body %}
    <h1>CorpsMetiers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ corps_metier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ corps_metier.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_corps_metiers_index') }}">back to list</a>

    <a href="{{ path('app_corps_metiers_edit', {'id': corps_metier.id}) }}">edit</a>

    {{ include('corps_metiers/_delete_form.html.twig') }}
{% endblock %}
