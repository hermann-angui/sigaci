<!DOCTYPE html>

<title>BrancheMetier</title>

{% block body %}
    <h1>BrancheMetier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ branche_metier.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_branche_metier_index') }}">back to list</a>

    <a href="{{ path('app_branche_metier_edit', {'id': branche_metier.id}) }}">edit</a>

    {{ include('branche_metier/_delete_form.html.twig') }}
{% endblock %}
