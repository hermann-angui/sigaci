<form method="post" action="{{ path('app_branche_metier_delete', {'id': branche_metier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ branche_metier.id) }}">
    <button class="btn">Delete</button>
</form>
