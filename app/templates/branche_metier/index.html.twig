<!DOCTYPE html>

<title>BrancheMetier index</title>

{% block body %}
    <h1>BrancheMetier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for branche_metier in branche_metiers %}
            <tr>
                <td>{{ branche_metier.id }}</td>
                <td>
                    <a href="{{ path('app_branche_metier_show', {'id': branche_metier.id}) }}">show</a>
                    <a href="{{ path('app_branche_metier_edit', {'id': branche_metier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_branche_metier_new') }}">Create new</a>
{% endblock %}
