<!DOCTYPE html>

<title>Crm</title>

{% block body %}
    <h1>Crm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crm.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crm.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crm_index') }}">back to list</a>

    <a href="{{ path('app_crm_edit', {'id': crm.id}) }}">edit</a>

    {{ include('crm/_delete_form.html.twig') }}
{% endblock %}
